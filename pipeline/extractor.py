"""
extractor.py

PDF content extraction using Docling.
Handles both native digital PDFs and scanned documents (OCR).
Splits extracted text into semantic chunks and attaches metadata
(page number, document name, chunk index) to each chunk.
"""
